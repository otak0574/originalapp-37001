<p style="color: green"><%= notice %></p>
<div class="container">
  <div class="card">
    <div class="card-body">
      <div class="card-title h5"><strong>現在のカート</strong></div>
      <% @cart.cart_items.each do |item| %>
        <div>
          <%= item.item.name %> × <%= item.quantity %>
        </div>
      <% end %>
      <div class="mt-4">
        <%= link_to "カートの編集", edit_cart_path(@cart), class: "btn btn-warning" %>
        <%= link_to "カート一覧", carts_path, class: "btn btn-secondary" %>
        <%= link_to "買い物に戻る", publicstore_path(@stores), class: "btn btn-secondary" %>
      </div>
      <div class="mt-4">
        <%= button_to "カートを削除", @cart, method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>